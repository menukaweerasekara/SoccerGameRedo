@page
@model SoccerGame.Pages.Divisions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Division[0].Division_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Division[0].Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Division) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Division_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DivisionID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DivisionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DivisionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
